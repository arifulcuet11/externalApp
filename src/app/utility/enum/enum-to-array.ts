// interface EnumItem<E> { id: E; name: string; }

// function enumToArray<E>(Enum: any): EnumItem<E>[] {
//     return Object.keys(Enum).map(key => ({ id: Enum[key], name: key } as EnumItem<E>))
// }

// function enumToArray(Enum: any) {
//     return Object.entries(Enum).map(([key, value]) => ({ id: key, name: value }));
// }

