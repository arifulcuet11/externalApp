import { LanguageCheckPipe } from './language-check.pipe';

describe('LanguageCheckPipe', () => {
  it('create an instance', () => {
    const pipe = new LanguageCheckPipe();
    expect(pipe).toBeTruthy();
  });
});
