import { MinuteConversionPipe } from './minute-conversion.pipe';

describe('MinuteConversionPipe', () => {
  it('create an instance', () => {
    const pipe = new MinuteConversionPipe();
    expect(pipe).toBeTruthy();
  });
});
